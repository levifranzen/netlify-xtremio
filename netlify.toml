[build]
  command = "npm run build"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Uma única function roteia tudo, mantendo o addon simples.
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/addon/:splat"
  status = 200
